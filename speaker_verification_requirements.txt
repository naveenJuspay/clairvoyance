# Additional requirements for Speaker Verification System
# Add these to your existing requirements.txt

# Core dependencies for speaker verification
speechbrain>=0.5.15
torch>=2.0.0
torchaudio>=2.0.0
librosa>=0.10.0
soundfile>=0.12.0
scikit-learn>=1.3.0
scipy>=1.11.0
numpy>=1.24.0

# Speaker diarization (recommended for multi-speaker scenarios)
pyannote.audio>=3.1.0

# Optional dependencies for pyannote.audio
huggingface-hub>=0.16.0  # For pyannote model authentication
transformers>=4.21.0     # Required by pyannote.audio

# Optional: for GPU acceleration (if available)
# torch-audio # Alternative to torchaudio
# torchvision # Sometimes required by torch ecosystem

# Installation notes:
# 1. For pyannote.audio, you may need to accept user conditions on HuggingFace:
#    - pyannote/segmentation-3.0
#    - pyannote/speaker-diarization-3.1
# 2. Authentication options (choose one):
#    - Add HF_TOKEN=your_token_here to your .env file (recommended)
#    - Run `huggingface-hub login` command line tool
#    - Set HF_TOKEN environment variable manually